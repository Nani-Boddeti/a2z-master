/*
package com.a2z.persistence;

import com.a2z.dao.RootEntity;
import jakarta.persistence.EntityListeners;
import jakarta.persistence.PrePersist;
import jakarta.persistence.PreUpdate;
import org.springframework.stereotype.Component;

//@Component
@EntityListeners({})
public class EntityPersistanceManager {
    @PrePersist
    public void setCreationTime(Object entity) {
        // Logic to execute before persisting any entity
       // ((RootEntity)entity).setCreationTime(new java.util.Date());
    }
    @PreUpdate
    public void setModifiedTime(Object entity) {
        // Logic to execute before updating any entity
       // ((RootEntity)entity).setModifiedTime(new java.util.Date());
    }

}
*/
